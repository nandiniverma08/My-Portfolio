import React from "react";

export const Projects = () => {
  return (
    <section>
      <h2>Projects</h2>
      {/* <div>
        {Projects.map((project, id) => {
          return <div key={id}></div>;
        })}
      </div> */}
    </section>
  );
};
